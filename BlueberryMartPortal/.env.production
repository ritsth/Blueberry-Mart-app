# Used automatically by `vite build` (production). The API URL is baked into the
# client bundle and is public anyway, so it's fine to commit.
VITE_API_URL=https://blueberrymart-api-278293545480.us-central1.run.app
