EXPO_PUBLIC_API_URL=http://localhost:5027
# Google OAuth *Web* client id — required for "Continue with Google". Must match the API's
# Google__WebClientId. (Read from .env when serving via Metro; from eas.json `env` in EAS builds.)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
